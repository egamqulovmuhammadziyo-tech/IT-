{
  "name": "beshariq-it-center",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site with Telegram lead backend",
  "main": "server.js",
  "scripts": {
    "build": "node -e \"console.log('Netlify build ready')\"",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check lead-service.js && node --check api/leads.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2"
  }
}
